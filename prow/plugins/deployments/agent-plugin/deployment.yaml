# Autogenerated. Do NOT update Manually
# Last generated on 2025-11-28 02:25:20.
#
apiVersion: apps/v1
kind: Deployment
metadata:
  name: agent-plugin
  namespace: prow
  labels:
    app: agent-plugin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agent-plugin
  template:
    metadata:
      labels:
        app: agent-plugin
    spec:
      serviceAccountName: agent-plugin
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: webhook-server
        image: public.ecr.aws/m5q3e4b2/prow:agent-plugin-0.0.3
        ports:
        - containerPort: 8080
          name: http
        command: ["./webhook-server"]
        args:
        - --port=8080
        - --allowed-team=ack-chart-maintainers
        - --s3-bucket-name=s3://ack-prow-logs
        - --github-app-id=$(GITHUB_APP_ID)
        - --github-app-private-key-path=/etc/github/cert
        - --github-endpoint=http://ghproxy
        - --github-endpoint=https://api.github.com
        - --dry-run=false
        env:
        - name: GITHUB_APP_ID
          valueFrom:
            secretKeyRef:
              name: github-token
              key: appid
        - name: PROW_JOB_ID
          value: "prow-jobs"
        - name: PROW_JOB_NAMESPACE
          value: "prow"
        volumeMounts:
        - name: workflows-config
          mountPath: /etc/workflows
          readOnly: true
        - name: github-token
          mountPath: /etc/github
          readOnly: true
        - name: hmac
          mountPath: /etc/webhook
          readOnly: true
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          capabilities:
            drop:
            - ALL
      volumes:
      - name: workflows-config
        configMap:
          name: agent-workflow-config
      - name: github-token
        secret:
          secretName: github-token
      - name: hmac
        secret:
          secretName: hmac-token