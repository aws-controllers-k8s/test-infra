apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: ack-soak-test
  region: us-west-2

nodeGroups:
  - name: ng-1
    instanceType: m5.xlarge
    desiredCapacity: 2
    volumeSize: 80
