# Prow secrets (SecretProviderClass + RBAC) - depends on the Secrets Store CSI
# addon being installed by ACK via self-managed-cluster.
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: prow-secrets
  namespace: flux-system
spec:
  interval: 5m
  sourceRef:
    kind: GitRepository
    name: test-infra
  path: ./flux/self-managed/prow-secrets
  prune: true
  postBuild:
    substituteFrom:
    - kind: ConfigMap
      name: self-managed-vars
  dependsOn:
  - name: self-managed-cluster
