apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# infra/, roles/ and pod-identities/ are no longer listed: their manifests moved
# into flux/ack/charts/ack-build-infra as template subdirectories, rendered by
# the HelmRelease below.
resources:
- helm-release.yaml
